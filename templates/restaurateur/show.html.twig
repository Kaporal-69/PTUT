{% extends 'base.html.twig' %}

{% block title %}Restaurateur{% endblock %}

{% block body %}
    <h1>Restaurateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurateur.id }}</td>
            </tr>
            <tr>
                <th>Site</th>
                <td>{{ restaurateur.site }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ restaurateur.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('restaurateur_index') }}">back to list</a>

    <a href="{{ path('restaurateur_edit', {'id': restaurateur.id}) }}">edit</a>

    {{ include('restaurateur/_delete_form.html.twig') }}
{% endblock %}
